{
  "name": "merchantpilot",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^2.0.0"
  }
}
